<% if signed_in? %>
<h4>Add a review: </h4>
<%= form_for([@post, @post.reviews.build]) do |f| %>
<p>
<%= f.label :body, "Reviews" %><br>
<%= f.text_area :body %>
</p>
<p>
<%= f.submit "Submit"%>
</p>
<%end%>
<%end%>