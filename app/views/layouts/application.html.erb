<!DOCTYPE html>
<html>
  <head>
    <title>roadrunner</title>
    <%= csrf_meta_tags %>

    <meta name="viewport" content="width=device-width, initial-scale=1">

   <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
     <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.0/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>


  </head>

  <body>
    <div  class="container">
          <nav class="navbar navbar-expand-lg navbar-light justify-content-between">
              <h4 class="nav-item"><%= link_to('roadrunner',simple_pages_index_path)%></h4>
              <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
              </button>

              <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
                <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                  <li class="nav-item active"><h4 class="nav-link">
                    <span class="sr-only">(current)</span></h4>
                  </li>
                        <li class="nav-item"><h4 class="nav-link">
                            <%= link_to('about',simple_pages_about_path) %></h4>
                  </li>
                  <li class="0nav-item"><h4 class="nav-link">
                    <%= link_to('contact', simple_pages_contact_path)%></h4>
                  </li>
                  <li class="0nav-item"><h4 class="nav-link">
                    <%= link_to('articles', posts_path)%></h4>
                  </li>

                </ul>
                <%= form_with(url: posts_path, method: 'get', local: true) do |form|%>
                    <div class="form-inline my-2 my-lg-0">
                        <%= form.text_field :q, id: 'q', value: params[:q], class: "form-control mr-sm-2", type: "search", placeholder:"search"%>
                        <%= form.submit 'Search', class: "btn btn-outline-success my-2 my-sm-0", type: "submit"%>
                    </div>
                    
                <%end%>
                <ul class= "nav pull-right">
                <li class= "nav-item">
                <% if user_signed_in? %>
                <%= link_to("logout", destroy_user_session_path, method: :delete, class: "nav-link")%>
                <% else%>
                <%= link_to("login", new_user_session_path, class: "nav-link") %>
                <%end%>
                </li>
                </ul>
        </nav>
    </div>
    <% if notice %>
<p id="notice" class="alert alert-info" role = "alert"><%= notice%></p>
<%elsif alert %>
<p class="alert alert-info"><%=alert %></p>
<% end %>
    <%= yield %>
    <footer>
    	&copy; roadrunner
    </footer>
  </body>
</html>
